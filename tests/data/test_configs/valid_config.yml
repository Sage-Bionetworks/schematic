# This is a valid config, but all values are different from defaults

asset_store:
  synapse:
    config_basename: "file_name"
    manifest_basename: "file_name"
    master_fileview_id: "syn1"
    manifest_folder: "folder_name"

manifest:
  title: "title"
  data_type:
    - "data_type"

model:
  location: "model.jsonld"

google_sheets:
  service_acct_creds_synapse_id: "syn1"
  service_acct_creds_basename: "creds.json"
  master_template_id: ""
  strict_validation: false
